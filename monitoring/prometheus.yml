global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: profile-service
    static_configs:
      - targets:
          - profile-service:8002

  # Add other services here as they get instrumented:
  # - job_name: auth-service
  #   static_configs:
  #     - targets: [auth-service:8001]
